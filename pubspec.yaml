name: number_selector
description: A custom widget designed for choosing an integer number by buttons or textfield.
version: 1.0.4
homepage: https://github.com/guidotheelen/number_selector

environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
